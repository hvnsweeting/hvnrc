#math
	same as C, add some function:
	fact(), degrees(), hypot(), log()
	math.e, math.pi

#os
	show file stat, permission
	change dir, show current dir, parent dir
	make dir, rm file, dir
	walk throught dir
	creating process with os.folk()

#tempfile
	create temp file

#subprocess
	running external commands

#time
	sleep
#datetime
	get current time, date, datetime return corresponding obj
	format and convert between them
