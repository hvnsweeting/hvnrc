#!/usr/bin/env python

import re

if __name__ == '__main__':
	pattern = 'regexp|regexp not'
	string = 'regexp not'
	#Test whether python use regexp oriented or not
	x = re.match(pattern, string)
	if x.group() == 'regexp':
		print 'Python re is regexp oriented!'

