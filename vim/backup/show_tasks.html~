{% extends "layout.html" %}
{% block body %}
{% if session.logged_in %}
<form action="{{ url_for('add_task') }}" method=post class=add-entry>
	<dl>
		<dt>Task:
		<dd><input type=text size=30 name=task>
		<dt>Start: (dd-mm-yyyy)
		<dd><input type=text size=30 name=start>
		<dt>End:
		<dd><input type=text size=30 name=end>
		<dt>Priority:
		<dd><input type=text size=10 name=priority>
		<dd><input type=submit value=Share>
	</dl>
</form>

{% endif %}

<ul class=tasks>
	{% for task in tasks %}
	<li><h2>{{ task.task}} {{ "*" * task.priority}} </h2> 
	<h4>Start: {{ task.start }} - End: {{ task.end }}  </h4>
	<h4>Days left: {{ task.diff }} </h4>

	{% else %}
	<li><em> Unblieveable, no task here so far </em>
	{% endfor %}
</ul>
{% endblock %}
